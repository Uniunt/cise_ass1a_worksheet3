{
  "name": "cise_assia_worksheet3",
  "version": "1.0.0",
  "description": "",
  "main": "app.js",
  "scripts": {
    "heroku-prebuild": "npm install -g serve",
    "start": "node app.js && serve -s build",
    "heroku-postbuild": "npm run client-install && npm run client-build",
    "client-install": "cd my-app && npm install",
    "client-build": "cd my-app && npm run build"
  },
  "author": "Chaoyang Wang",
  "license": "ISC",
  "dependencies": {
    "bcryptjs": "^2.4.3",
    "body-parser": "^1.20.0",
    "config": "^3.3.7",
    "cors": "^2.8.5",
    "dotenv": "^16.0.1",
    "express": "^4.18.1",
    "mongoose": "^6.5.2",
    "validation": "^0.0.1"
  },
  "devDependencies": {
    "nodemon": "^2.0.19"
  }
}
